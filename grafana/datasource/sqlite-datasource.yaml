apiVersion: 1

datasources:
  - name: frser-sqlite-datasource
    type: frser-sqlite-datasource
    access: proxy
    jsonData:
      path: /var/lib/grafana/kpi_metrics.db
    isDefault: true
    editable: true
